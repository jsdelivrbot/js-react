import React from 'react';
import ReactDOM from 'react-dom';

const Hero = () => (
        <div className="hero">
            <div>
                <h1>TEST LANDING</h1>
                <h2>Choose an awesome game to start, or visit the store to get more!</h2>
            </div>
        </div>
        );

export default Hero;