import React from 'react';
import ReactDOM from 'react-dom';

const Shop = () => (
        <div className="hero">
            <div>
                <h1>SHOP SHOP SHOP</h1>
            </div>
        </div>
        );

export default Shop;