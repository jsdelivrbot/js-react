import React from 'react';
import ReactDOM from 'react-dom';

const Hero = () => (
        <div className="hero">
            <div>
                <h1>TEST LANDING</h1>
                
            </div>
        </div>
        );

export default Hero;
